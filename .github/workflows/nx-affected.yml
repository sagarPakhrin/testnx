name: Nx Distributed Tasks
on: push
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git fetch --no-tags --prune --depth=1 origin master
      - run: npm install
      - run: npm run nx affected --target=test --base=origin/main --parallel
      - run: npm run nx affected --target=lint --base=origin/main --parallel
      - run: npm run nx affected --target=build --base=origin/main --prod --parallel
